# Copy this file to .env and fill in real values.
# The .env file is gitignored — never commit secrets.

# Optional: connect to Meelo's Supabase for real data.
# Leave blank to use the mock data baked into server.py for the demo.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
